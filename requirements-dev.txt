# Development and CI dependencies: tests and linting.
# Deliberately NOT installed into the production image.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.3
httpx==0.27.2
ruff==0.6.8
