[pytest]
# Run from the repository root so `import app` resolves without the suite
# needing to know where it lives on disk.
testpaths = tests
pythonpath = .

# Show which test failed and why, without scrolling through passing ones.
addopts = --verbose --tb=short --strict-markers
